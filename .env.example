# Configurações do App Vitin para o Servidor
# Renomeie este arquivo para .env no seu ambiente de deploy (ou configure no painel do Render/Railway)

# Senha para acessar o painel do professor (padrão: vitin123)
ADMIN_PASSWORD=vitin123

# Porta do servidor (padrão: 5000, o Render/Railway costuma sobrescrever via PORT)
PORT=5000
